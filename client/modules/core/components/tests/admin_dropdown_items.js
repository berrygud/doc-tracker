const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import AdminDropdownItems from '../admin_dropdown_items';

describe('core.components.admin_dropdown_items', () => {
  it('should do something');
});
