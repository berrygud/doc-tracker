const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import DocumentTypeDropdown from '../document_type_dropdown';

describe('admin.components.document_type_dropdown', () => {
  it('should do something');
});
