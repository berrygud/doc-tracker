const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import SchoolDistrictDropdown from '../school_district_dropdown';

describe('admin.components.school_district_dropdown', () => {
  it('should do something');
});
