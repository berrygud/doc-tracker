const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import ExcelExport from '../excel_export';

describe('admin.components.excel_export', () => {
  it('should do something');
});
