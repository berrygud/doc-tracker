const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import SchoolDistrictList from '../school_district_list';

describe('admin.components.school_district_list', () => {
  it('should do something');
});
