const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import SchoolDistrictEdit from '../school_district_edit';

describe('admin.components.school_district_edit', () => {
  it('should do something');
});
